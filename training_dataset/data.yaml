# Dataset paths
train: ./training_dataset/train
val: ./training_dataset/val
test: ./training_dataset/test    # Path to test images (optional)

# Parameters
nc: 1  # Number of classes (1 in this case for 'person')
names: ["person"]  # Class names
# kpt_shape: [4, 3]  # 12 keypoints, each with x, y, and confidence


